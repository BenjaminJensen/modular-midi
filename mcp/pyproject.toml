[project]
name = "modular-midi-mcp"
version = "0.1.0"
description = "MCP server wrapping the modular-midi Docker build/test/lint workflow"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.2.0",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
